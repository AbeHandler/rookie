CREATE OR REPLACE FUNCTION get_counts(input character varying) RETURNS TABLE (string character varying, c bigint) AS $$                                                                       BEGIN                                                                           RETURN QUERY                                             select * from (select s2, count(*) c from get_links(input) group by s2) as e order by c desc;                                          END;                                                                            $$ LANGUAGE plpgsql;